# Local test wiki (never commit user data to the repo)
test-wiki/
.exovibe-local/

# Internal planning docs (not public before hackathon submission)
# After the hackathon, to be reworked into docs/ARCHITECTURE.md for public release
prd.md
marketing/

# Local exovibe runtime artifacts (real user data on the user's machine)
# ~/.claude/exovibe/ lives in $HOME, but block it if it ever appears inside the repo
exovibe-runtime/
*.exovibe.local.json

# Node
node_modules/
npm-debug.log
.pnpm-debug.log
yarn-debug.log
yarn-error.log

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local session settings (per-user allowlist — do not share)
.claude/settings.local.json

# Build artifacts
dist/
build/
*.tar.gz

# Environment / secrets
.env
.env.*
!.env.example

# Temp files
*.tmp
*.log

# Playwright MCP auto-generated artifacts (screenshot archive)
.playwright-mcp/

# Regenerable artifacts in the demo vault (source is committed, dashboard is script-generated)
docs/demo-vault/dashboard.html
